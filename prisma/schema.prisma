generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model companysummaries {
  id      Int     @id @default(autoincrement())
  site    String?
  summary String?
}
